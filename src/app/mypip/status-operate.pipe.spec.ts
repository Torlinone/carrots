import { StatusOperatePipe } from './status-operate.pipe';

describe('StatusOperatePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusOperatePipe();
    expect(pipe).toBeTruthy();
  });
});
